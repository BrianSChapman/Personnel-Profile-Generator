const { it, describe } = require("node:test");
const Employee = require("../lib/employee");

describe("Employee", () => {
    describe("Initialization"), () => {
        it("should return a constructor containing a  ")
    }

})